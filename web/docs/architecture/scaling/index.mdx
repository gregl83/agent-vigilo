---
title: Scaling
sidebar_label: Scaling
sidebar_position: 0
description: Logical shard model, single-cluster startup posture, and migration runbook for expanding Agent Vigilo execution data across larger PostgreSQL capacity.
keywords:
  - Agent Vigilo scale out
  - evaluation shard migration
  - PostgreSQL partitioning
  - distributed evaluation scaling
---

# Scaling

Agent Vigilo starts as a single PostgreSQL deployment, but high-volume execution data is keyed by a logical `run_shard` so large runs can spread across 128 shard partitions and later move to additional database placements.

## Current Implementation Status

Implemented today:

- 128 logical `run_shard` values.
- Shard-partitioned high-volume execution tables inside one PostgreSQL database.
- Placement catalog tables:
  `database_placements` and `shard_placements`.
- A seeded default placement:
  `primary -> DATABASE_URL` with role `control_and_shard`.
- Runtime placement configuration for the control alias and default shard alias.
- Placement catalog validation during `vigilo setup` after migrations run.
- Chunk-ready messages that carry `run_id`, `run_shard`, and `chunk_id`.
- Worker hot-path queries that use `run_id + run_shard` for chunk-local processing.
- Coordinator dispatch cursors keyed by `run_id + run_shard`, so dispatch windows scan one shard partition at a time and large runs rotate across open shard cursors.

Not implemented yet:

- Multiple PostgreSQL placement routing at runtime.
- A runtime `db_for_shard(run_id, run_shard)` resolver.
- Automatic creation of `shard_placements` rows during run creation.
- Multi-placement assignment policy at run creation.
- Coordinator ownership by database placement or global shard range.
- Cross-database run finalization, cancellation, result export, or status fanout.
- Automated shard migration tooling.

The current system should be read as **logical sharding on a single PostgreSQL deployment**, with placement metadata, schema, and message contracts prepared for future physical shard routing.

## Terminology

A database placement is a named PostgreSQL target. The placement `alias` is stable routing metadata, and `database_url_env` names the environment variable that contains the actual connection URL.

A logical shard is a `run_shard` value in the range `0..127`. It partitions run execution data and stays stable for the lifetime of the run shard.

A shard placement maps one `(run_id, run_shard)` pair to a database placement alias. Moving data to another database changes the placement row, not the `run_shard` stored on execution rows.

Placement roles describe what kind of data a database may hold:

- `control`: authoritative control-plane data such as global run metadata and routing catalog rows.
- `shard`: shard-local execution data such as chunks, executions, attempts, aggregates, evaluator results, and shard-local outbox events.
- `control_and_shard`: both roles in one database. This is the default single-database deployment.

Only one active control-capable placement may exist at a time. In schema terms, there can be at most one active placement whose role is `control` or `control_and_shard`. Multiple active `shard` placements are allowed.

## Current Shard Model

The system uses 128 logical shards:

```text
run_shard = 0..127
```

Run chunks receive a shard during run creation. Every execution, attempt, aggregate, and evaluator result produced from that chunk carries the same shard key:

```text
run_chunks(run_id, run_shard, id)
run_shard_dispatch_cursors(run_id, run_shard)
executions(run_id, run_shard, id)
execution_attempts(run_id, run_shard, id)
execution_aggregates(run_id, run_shard, execution_id)
evaluator_results(run_id, run_shard, id)
```

The high-volume tables are list partitioned by `run_shard`. A worker that owns one chunk can query by `run_id + run_shard + chunk_id`, keeping the chunk-local workflow on one partition per table.

## Small Deployment

The default deployment can keep every logical shard on one PostgreSQL cluster:

```text
run_shard 0..127 -> primary PostgreSQL
```

No multi-cluster routing is required to start. The shard key still matters because it prevents one massive run from concentrating all execution writes into one physical partition.

## Queue and Worker Routing

Chunk-ready messages include the routing key:

```json
{
  "run_id": "00000000-0000-0000-0000-000000000000",
  "run_shard": 42,
  "chunk_id": "00000000-0000-0000-0000-000000000000"
}
```

Workers use `run_id + run_shard + chunk_id` to claim the chunk. In the single-cluster deployment, the database resolver is trivial because every shard points at the same pool. In a future multi-cluster deployment, the resolver should map `run_id + run_shard` to a database placement before the worker opens a transaction.

## Coordinator Dispatch Routing

Run creation stores one `run_shard_dispatch_cursors` row for every logical shard used by the run. Each coordinator dispatch window claims one open cursor with `FOR UPDATE SKIP LOCKED`, selects pending chunks only for that `run_id + run_shard`, and then either releases the cursor for another window or marks it drained when no undispatched pending chunks remain. Dispatch updates the parent `runs` row only when starting a pending run; already-running runs dispatch by cursor while taking only a compatible shared run-row lock.

This means a single dispatch statement is shard-local. A large run is not forced onto one shard; its chunks remain spread across the 128 logical shard range. Because open cursors are ordered by cursor update time, repeated dispatch windows rotate across a run's active shards instead of draining the lowest shard first. Multiple coordinators can claim different open shard cursors for the same already-running run without taking incompatible run-row locks, while cancellation and finalization still serialize through their exclusive lifecycle updates.

## Placement Layer

Placement metadata exists in the database even for the default single-database deployment:

```sql
CREATE TABLE database_placements (
    alias TEXT PRIMARY KEY,
    database_url_env TEXT NOT NULL,
    role TEXT NOT NULL CHECK (role IN ('control', 'shard', 'control_and_shard')),
    status TEXT NOT NULL CHECK (status IN ('active', 'disabled')),
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE shard_placements (
    run_id UUID NOT NULL,
    run_shard SMALLINT NOT NULL CHECK (run_shard >= 0 AND run_shard < 128),
    database_alias TEXT NOT NULL REFERENCES database_placements(alias),
    status TEXT NOT NULL CHECK (status IN ('active', 'moving', 'draining')),
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    PRIMARY KEY (run_id, run_shard)
);
```

Fresh databases seed one placement row:

```text
alias = primary
database_url_env = DATABASE_URL
role = control_and_shard
status = active
```

Until runtime routing is implemented, every workflow still uses the single configured database connection. The placement tables are the catalog that later phases will use to map each `run_id + run_shard` pair to a database alias. Expansion changes placement rows, not the logical shard value stored in execution data.

## Runtime Placement Configuration

The `database_placements` table is the authoritative placement catalog. Environment variables provide secret connection values and process-local defaults; they must not duplicate the placement catalog.

Default single-database configuration:

```text
DATABASE_URL=postgres://...
VIGILO_CONTROL_DATABASE_ALIAS=primary
VIGILO_DEFAULT_SHARD_DATABASE_ALIAS=primary
```

`DATABASE_URL` connects to the active control-capable placement. The seeded `primary` row uses `database_url_env = DATABASE_URL`, so command-line `--database-url` or the `DATABASE_URL` env var supplies that connection value.

When adding a shard placement, insert a `database_placements` row and deploy the matching secret env var:

```text
alias = shard_001
database_url_env = VIGILO_SHARD_001_DATABASE_URL
role = shard
status = active

VIGILO_SHARD_001_DATABASE_URL=postgres://...
```

The initial run-shard assignment rule is deliberately simple: every new run shard maps to `VIGILO_DEFAULT_SHARD_DATABASE_ALIAS` when placement rows are created. Richer assignment policies should be introduced only with an explicit admin/config contract, because routing must read stored `shard_placements` rows instead of recalculating from current placement count.

## Migration Runbook

Use this procedure when moving logical shards to a new PostgreSQL placement.

1. **Choose shard scope.** Select one or more `run_id + run_shard` pairs, or a whole `run_shard` range if the placement layer supports global shard ownership.
2. **Mark placement moving.** Set selected placement rows to `moving` so coordinators stop dispatching new chunks for those shards. Existing workers may finish their current leases.
3. **Drain open work.** Wait until selected chunks are no longer `leased`, or cancel/recover leases according to the chunk recovery policy.
4. **Copy shard-owned rows.** Move rows for the selected shard scope from the source placement to the target placement:

```text
run_chunks
run_shard_dispatch_cursors
executions
execution_attempts
execution_aggregates
evaluator_results
```

5. **Verify data.** Compare row counts and checksums per table for the selected `run_id + run_shard` scope.
6. **Switch placement.** Update placement rows to `active` on the new `database_alias`.
7. **Resume dispatch.** Coordinators and workers resolve the same `run_id + run_shard` to the new database placement and continue processing.
8. **Retain then clean up.** Keep source rows read-only for a retention window, then delete them after verification and backup policy allow it.

## Migration Standards

- `run_shard` is stable for the lifetime of a chunk and its child rows.
- Do not recalculate `run_shard` when adding database capacity.
- Queue messages must include `run_shard`; workers should not rediscover it with an extra lookup on the hot path.
- Coordinator dispatch should claim `run_shard_dispatch_cursors` and select chunks by `run_id + run_shard`, not scan all chunks for a run.
- Chunk-local worker queries should include `run_id` and `run_shard`.
- Joins between shard-partitioned run tables must use the full logical key, including `run_shard` (for example `run_id + run_shard + execution_id`), so result queries preserve partition locality and cannot cross-match rows from different shards.
- Run-level reporting may fan out across all 128 shards for the run.
- Large payload artifacts should be moved outside PostgreSQL before cross-cluster movement becomes routine.

## Recreating a Database

For a fresh deployment, apply the original migration files normally. The 128-shard schema is part of the create-table migrations, so no separate `ALTER TABLE` migration is required when recreating the database.
