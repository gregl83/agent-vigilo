---
title: Start
sidebar_label: Start
sidebar_position: 2
description: Flow diagrams for the long-running Agent Vigilo coordinator process and its orchestration loop.
keywords:
  - vigilo coordinator start
  - coordinator start flow
  - evaluation coordinator loop
  - distributed evaluation orchestration
  - run finalization loop
---

import Mermaid from '@theme/Mermaid';
import executionFlowDiagram from './execution-flow.mmd';
import decisionFlowDiagram from './decision-flow.mmd';

# Coordinator Start Command

How `vigilo coordinator start` runs configurable coordinator cycles continuously
until shutdown or service failure. Each cycle resumes a bounded number of
expired `creating` runs before chunk lease recovery and normal dispatch.

Dispatch claims one open `run_id + run_shard` cursor per window. Dispatch and
recovery insert one `run.chunk.ready` outbox event record per chunk, including
the chunk's `run_shard`, execution `database_alias`, and `write_epoch` in the
payload. Publication preserves that route hint so workers can claim
chunk-local work without an extra control-database routing lookup.

## Execution Flow

<Mermaid value={executionFlowDiagram} />

## Decision Flow

<Mermaid value={decisionFlowDiagram} />

## Related

- [Coordinator once flow](/docs/architecture/flows/coordinator/once/)
- [Worker start flow](/docs/architecture/flows/worker/start/)
- [Run create flow](/docs/architecture/flows/run/create/)
- [Run watch flow](/docs/architecture/flows/run/watch/)
- [Coordinator component](/docs/architecture/structure/components/coordinator/)
- [Container view](/docs/architecture/structure/containers/agent-vigilo/)
- [Glossary](/docs/glossary)
