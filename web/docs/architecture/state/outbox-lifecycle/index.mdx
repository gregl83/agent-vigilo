---
title: Outbox Lifecycle
sidebar_label: Outbox
sidebar_position: 4
description: State diagram for Agent Vigilo outbox lifecycle, dedupe guards, publication leases, retries, published events, and failures.
keywords:
  - Agent Vigilo outbox lifecycle
  - outbox state diagram
  - event publication flow
  - dedupe key guard
  - durable event publishing
---

import Mermaid from '@theme/Mermaid';
import outboxLifecycleDiagram from './diagram.mmd';

# Outbox Lifecycle

How durable <GlossaryTerm term="outbox-event-record">outbox event records</GlossaryTerm>
and hot <GlossaryTerm term="outbox-delivery-row">delivery rows</GlossaryTerm>
make state-change events publishable, retryable, and idempotent.
A <GlossaryTerm term="publish-claim">publish claim</GlossaryTerm> fences each
publisher, while the <GlossaryTerm term="dedupe-key">dedupe key</GlossaryTerm>
identifies the logical event.

<Mermaid value={outboxLifecycleDiagram} />

## Notes

- State workflows insert durable event records into `outbox_events` in the same database transaction as the state change.
- An insert trigger creates one `outbox_delivery_queue` row for each new event record; this is the hot queue scanned by coordinators.
- The publisher claims delivery rows with `available_at <= now()` and `FOR UPDATE SKIP LOCKED`, using the sharded delivery queue index for the hot claim path.
- A claim assigns a `claim_token`, sets `claimed_until`, and moves the delivery row `available_at` into the future as a publish lease.
- Claimed batches are published with bounded concurrency controlled by the coordinator outbox publish parallelism setting.
- RabbitMQ publication declares durable queues and bindings before publish,
  uses persistent <GlossaryTerm term="broker-message">broker messages</GlossaryTerm>,
  sets the outbox `dedupe_key` as the AMQP message ID,
  publishes with mandatory routing, and waits for a
  <GlossaryTerm term="publisher-confirm">publisher confirmation</GlossaryTerm>.
- Successful publishes mark the ledger event record `published` and delete the delivery row only when the delivery row still has the same `claim_token`; failed publishes clear the claim and return the delivery row to retry with a delay and error message.
- Broker delivery is
  <GlossaryTerm term="at-least-once-delivery">at-least-once</GlossaryTerm>.
  Exactly-once effects are managed by idempotency guards: outbox `dedupe_key`,
  fenced publisher claims, chunk-claim checks, current-attempt and
  live-attempt-lease authority checks, and evaluator-result uniqueness.

## Related

- [Coordinator once flow](/docs/architecture/flows/coordinator/once/)
- [Coordinator start flow](/docs/architecture/flows/coordinator/start/)
- [Run lifecycle](/docs/architecture/state/run-lifecycle/)
- [State ownership](/docs/architecture/state/ownership/)
