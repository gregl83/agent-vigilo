---
title: Worker Components
description: Component diagram for Agent Vigilo worker chunk claiming, agent calls, evaluator execution, result persistence, and retries.
keywords:
  - worker components
  - Agent Vigilo worker internals
  - evaluator execution component
  - chunk claim component
  - result persistence component
---

import Mermaid from '@theme/Mermaid';
import workerDiagram from './diagram.mmd';

# Worker Components

Describes the major internal responsibilities of the worker runtime.

<Mermaid value={workerDiagram} />

## Notes

- Workers consume `run.chunk.ready` messages and claim chunk leases before loading case data.
- Run context and evaluator components are cached inside the worker process to avoid repeated profile parsing and WASM compilation.
- Execution writes use shared run-state guards so chunks for the same running run do not serialize on the run row.
- Execution terminal transitions are authority guarded by the current attempt id and attempt number.
- Queue messages are acknowledged only after invalid, stale, or completed work is handled; recoverable chunk failures release the lease and requeue the message.

## Related

- [Structure overview](../..)
- [Container view](../../containers)
- [Worker flows](../../../flows/worker)
- [Chunk lifecycle](../../../state/chunk-lifecycle)
- [Execution lifecycle](../../../state/execution-lifecycle)
- [State ownership](../../../state/ownership)
- [Failure modes](../../../failure-modes)
- [Vigilo components](../vigilo)
- [Coordinator components](../coordinator)
