---
title: Agent Vigilo Containers
description: Container diagram for Agent Vigilo deployable runtime boundaries, persistence, queues, and evaluator execution.
keywords:
  - Agent Vigilo container diagram
  - distributed evaluation containers
  - CLI coordinator worker
  - message broker architecture
  - evaluator runtime container
---

import Mermaid from '@theme/Mermaid';
import containerDiagram from './diagram.mmd';

# Agent Vigilo Containers

This view shows each deployable <GlossaryTerm term="container">container</GlossaryTerm>:
the CLI, <GlossaryTerm term="coordinator">coordinator</GlossaryTerm>,
<GlossaryTerm term="worker">workers</GlossaryTerm>, PostgreSQL
<GlossaryTerm term="database-placement">database placements</GlossaryTerm>, the
RabbitMQ <GlossaryTerm term="broker">broker</GlossaryTerm>, and the external
<GlossaryTerm term="agent-target">agent target</GlossaryTerm> being evaluated.

<Mermaid value={containerDiagram} />

## Notes

- The CLI, coordinator, and worker are modes of the same Rust application but
  run as separate processes in distributed deployments.
- Database placements hold durable authority. RabbitMQ carries at-least-once
  broker deliveries but does not replace database claims or leases.
- The coordinator inserts outbox event records in PostgreSQL and publishes them
  as broker messages. Workers claim the chunks named by those messages before
  doing work.
- The external agent target is outside the Vigilo trust and persistence
  boundary. Wasm evaluators execute inside worker processes.

## Related

- [Runtime flow](../../../flow)
- [Failure modes](../../../failure-modes)
